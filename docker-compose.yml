version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: simpletrade
      POSTGRES_PASSWORD: simpletrade
      POSTGRES_DB: simpletrade
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
